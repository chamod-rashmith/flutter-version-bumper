name: flutter_version_bumper
description: A command-line tool to easily bump Flutter project versions using SemVer and build numbers.
version: 1.0.1
repository: https://github.com/chamod-rashmith/flutter-version-bumper
environment:
  sdk: '>=3.0.0 <4.0.0'

executables:
  fvb:

dependencies:
  args: ^2.4.2
  path: ^1.8.3
  yaml: ^3.1.2

dev_dependencies:
  lints: ^2.1.1
  test: ^1.24.0
